/*
Al presionar el botón pedir 5 números e informar la suma acumulada y el promedio.

- repetir entradas (5 veces) y suma
- se calcula solo un promedio

contador= contador(variable)+constante
			cuenta iteraciones, sucesos.

acumulador=acumulador(variable)+variable(valor)
			al valor guardado le acumula nuevos valores
*/

function mostrar()
{
	let acumulador;
	let contador;
	let numeroIngresado;
	let i;

	acumulador=0;
	contador=0;
	i=0;

	while(i<6)

}





































/*
function mostrar()
{
	var contador;
	var acumulador;
	var numeroIngresado;

	contador=0;
	acumulador=0;

	while(contador<5)
	{
		numeroIngresado=prompt("Ingrese un numero");
		numeroIngresado=parseInt(numeroIngresado);

		acumulador=acumulador+numeroIngresado;

		contador=contador+1;
		//contador++;
	}
	
	document.getElementById("txtIdSuma").value=acumulador;
	document.getElementById("txtIdPromedio").value=acumulador/contador; //con contados: ya que promedia con la cantidad de numeros que se han ingresado
	//txtIdSuma.value=acumulador;
	//txtIdPromedio.value=acumulador/contador;
}//FIN DE LA FUNCIÓN*/